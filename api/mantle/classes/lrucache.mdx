---
title: "Class: LRUCache<K, V>"
description: "Defined in: src/mantle/utils/cache.ts:56"
icon: "cube"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / LRUCache

# Class: LRUCache\<K, V\>

Defined in: src/mantle/utils/cache.ts:56

LRU Cache with optional TTL support and metrics

## Type Parameters

### K

`K`

### V

`V`

## Constructors

### Constructor

```ts
new LRUCache<K, V>(options): LRUCache<K, V>;
```

Defined in: src/mantle/utils/cache.ts:72

#### Parameters

##### options

[`LRUCacheOptions`](../interfaces/LRUCacheOptions.md) = `{}`

#### Returns

`LRUCache`\<`K`, `V`\>

## Accessors

### memoryBytes

#### Get Signature

```ts
get memoryBytes(): number;
```

Defined in: src/mantle/utils/cache.ts:232

Get current memory usage

##### Returns

`number`

### size

#### Get Signature

```ts
get size(): number;
```

Defined in: src/mantle/utils/cache.ts:225

Get current cache size

##### Returns

`number`

## Methods

### clear()

```ts
clear(): void;
```

Defined in: src/mantle/utils/cache.ts:212

Clear all entries

#### Returns

`void`

### delete()

```ts
delete(key): boolean;
```

Defined in: src/mantle/utils/cache.ts:200

Delete a key from the cache

#### Parameters

##### key

`K`

#### Returns

`boolean`

### export()

```ts
export(): {
  expiresAt?: number;
  key: K;
  value: V;
}[];
```

Defined in: src/mantle/utils/cache.ts:336

Export cache to JSON-serializable format

#### Returns

\{
  `expiresAt?`: `number`;
  `key`: `K`;
  `value`: `V`;
\}[]

### forEach()

```ts
forEach(callback): void;
```

Defined in: src/mantle/utils/cache.ts:262

Iterate over entries

#### Parameters

##### callback

(`value`, `key`) => `void`

#### Returns

`void`

### get()

```ts
get(key): V | undefined;
```

Defined in: src/mantle/utils/cache.ts:85

Get a value from the cache

#### Parameters

##### key

`K`

#### Returns

`V` \| `undefined`

### getMetrics()

```ts
getMetrics(): CacheMetrics;
```

Defined in: src/mantle/utils/cache.ts:293

Get cache metrics

#### Returns

### getOrCompute()

```ts
getOrCompute(
   key, 
   compute, 
   ttl?): V;
```

Defined in: src/mantle/utils/cache.ts:112

Get value or compute if missing

#### Parameters

##### key

`K`

##### compute

() => `V`

##### ttl?

`number`

#### Returns

`V`

### getOrComputeAsync()

```ts
getOrComputeAsync(
   key, 
   compute, 
ttl?): Promise<V>;
```

Defined in: src/mantle/utils/cache.ts:126

Get value or compute async if missing

#### Parameters

##### key

`K`

##### compute

() => `Promise`\<`V`\>

##### ttl?

`number`

#### Returns

`Promise`\<`V`\>

### has()

```ts
has(key): boolean;
```

Defined in: src/mantle/utils/cache.ts:185

Check if key exists (and is not expired)

#### Parameters

##### key

`K`

#### Returns

`boolean`

### import()

```ts
import(entries): void;
```

Defined in: src/mantle/utils/cache.ts:355

Import entries from exported format

#### Parameters

##### entries

\{
  `expiresAt?`: `number`;
  `key`: `K`;
  `value`: `V`;
\}[]

#### Returns

`void`

### keys()

```ts
keys(): IterableIterator<K>;
```

Defined in: src/mantle/utils/cache.ts:239

Get all keys

#### Returns

`IterableIterator`\<`K`\>

### prune()

```ts
prune(): number;
```

Defined in: src/mantle/utils/cache.ts:276

Clean up expired entries

#### Returns

`number`

### resetMetrics()

```ts
resetMetrics(): void;
```

Defined in: src/mantle/utils/cache.ts:326

Reset metrics

#### Returns

`void`

### set()

```ts
set(
   key, 
   value, 
   ttl?): void;
```

Defined in: src/mantle/utils/cache.ts:144

Set a value in the cache

#### Parameters

##### key

`K`

##### value

`V`

##### ttl?

`number`

#### Returns

`void`

### values()

```ts
values(): V[];
```

Defined in: src/mantle/utils/cache.ts:246

Get all values

#### Returns

`V`[]
