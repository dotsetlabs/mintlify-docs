---
title: "Interface: ScanSummary"
description: "Defined in: src/mantle/output/formatters.ts:67"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / ScanSummary

# Interface: ScanSummary

Defined in: src/mantle/output/formatters.ts:67

Output Formatting Module

Format scan results in multiple output formats for CI/CD integration.

## Example

```typescript
import { format, formatSARIF, formatMarkdown } from '@dotsetlabs/mantle';

// Format as SARIF for GitHub Code Scanning
const sarif = format(scanResult, 'sarif');
fs.writeFileSync('results.sarif', sarif);

// Format as Markdown for PR comments
const markdown = formatMarkdown(scanResult, { includeContext: true });

// Format as JSON for API consumption
const json = format(scanResult, 'json', { prettyPrint: true });

// Get available formats
const formats = getSupportedFormats();
// ['text', 'json', 'sarif', 'csv', 'xml', 'junit', 'gitlab', 'github', 'markdown']
```

## Properties

### byCategory

```ts
byCategory: Record<string, number>;
```

Defined in: src/mantle/output/formatters.ts:73

### byType

```ts
byType: Record<string, number>;
```

Defined in: src/mantle/output/formatters.ts:74

### critical

```ts
critical: number;
```

Defined in: src/mantle/output/formatters.ts:68

### high

```ts
high: number;
```

Defined in: src/mantle/output/formatters.ts:69

### info

```ts
info: number;
```

Defined in: src/mantle/output/formatters.ts:72

### low

```ts
low: number;
```

Defined in: src/mantle/output/formatters.ts:71

### medium

```ts
medium: number;
```

Defined in: src/mantle/output/formatters.ts:70
