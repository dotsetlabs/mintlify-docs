---
title: "Interface: MemorySnapshot"
description: "Defined in: src/mantle/utils/profiler.ts:34"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / MemorySnapshot

# Interface: MemorySnapshot

Defined in: src/mantle/utils/profiler.ts:34

Performance Profiling Module

Measure and optimize scanning performance with profiling tools.

## Example

```typescript
import {
  Timer,
  Profiler,
  enableProfiling,
  formatDuration,
  formatBytes,
} from '@dotsetlabs/mantle';

// Enable global profiling
enableProfiling();

// Time a specific operation
const timer = new Timer();
timer.start();
await scanLargeFile();
timer.stop();
console.log(`Scan took: ${formatDuration(timer.elapsed())}`);

// Use profiler for detailed metrics
const profiler = new Profiler('scan-operation');
profiler.start('pattern-matching');
// ... pattern matching
profiler.end('pattern-matching');

profiler.start('entropy-analysis');
// ... entropy analysis
profiler.end('entropy-analysis');

console.log(profiler.getReport());
```

## Properties

### arrayBuffers

```ts
arrayBuffers: number;
```

Defined in: src/mantle/utils/profiler.ts:39

### external

```ts
external: number;
```

Defined in: src/mantle/utils/profiler.ts:38

### heapTotal

```ts
heapTotal: number;
```

Defined in: src/mantle/utils/profiler.ts:37

### heapUsed

```ts
heapUsed: number;
```

Defined in: src/mantle/utils/profiler.ts:36

### rss

```ts
rss: number;
```

Defined in: src/mantle/utils/profiler.ts:40

### timestamp

```ts
timestamp: Date;
```

Defined in: src/mantle/utils/profiler.ts:35
