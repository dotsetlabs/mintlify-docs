---
title: "Interface: LRUCacheOptions"
description: "Defined in: src/mantle/utils/cache.ts:34"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / LRUCacheOptions

# Interface: LRUCacheOptions

Defined in: src/mantle/utils/cache.ts:34

Caching Module

LRU caching and memoization for performance optimization.

## Example

```typescript
import {
  LRUCache,
  memoize,
  memoizeAsync,
  getPatternCacheMetrics,
} from '@dotsetlabs/mantle';

// Create an LRU cache
const cache = new LRUCache<string, RegExp>({ maxSize: 1000 });
cache.set('aws-key', /AKIA[A-Z0-9]{16}/g);

// Memoize expensive functions
const parsePattern = memoize((pattern: string) => {
  return new RegExp(pattern, 'gi');
});

// Memoize async functions
const fetchSecrets = memoizeAsync(async (key: string) => {
  return await secretsManager.getValue(key);
}, { ttlMs: 60000 });

// Check cache performance
const metrics = getPatternCacheMetrics();
console.log(`Cache hit rate: ${metrics.hitRate}%`);
```

## Properties

### defaultTtl?

```ts
optional defaultTtl: number;
```

Defined in: src/mantle/utils/cache.ts:38

Default TTL in milliseconds (optional)

### enableMetrics?

```ts
optional enableMetrics: boolean;
```

Defined in: src/mantle/utils/cache.ts:40

Enable metrics collection (default: true)

### maxMemoryBytes?

```ts
optional maxMemoryBytes: number;
```

Defined in: src/mantle/utils/cache.ts:44

Maximum memory size in bytes (optional)

### maxSize?

```ts
optional maxSize: number;
```

Defined in: src/mantle/utils/cache.ts:36

Maximum number of entries (default: 100)

### onEvict()?

```ts
optional onEvict: <T>(key, value) => void;
```

Defined in: src/mantle/utils/cache.ts:42

Called when entry is evicted

#### Type Parameters

##### T

`T`

#### Parameters

##### key

`unknown`

##### value

`T`

#### Returns

`void`

### sizeOf()?

```ts
optional sizeOf: <T>(value) => number;
```

Defined in: src/mantle/utils/cache.ts:46

Function to estimate entry size in bytes

#### Type Parameters

##### T

`T`

#### Parameters

##### value

`T`

#### Returns

`number`
