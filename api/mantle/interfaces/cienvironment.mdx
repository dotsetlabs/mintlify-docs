---
title: "Interface: CIEnvironment"
description: "Defined in: src/mantle/ci/index.ts:138"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / CIEnvironment

# Interface: CIEnvironment

Defined in: src/mantle/ci/index.ts:138

CI/CD Integration Module

Native integration with GitHub Actions, GitLab CI, and other CI providers.

## Example

```typescript
import {
  detectCIEnvironment,
  ExitCodes,
  determineExitCode,
  writeGitHubSummary,
  createGitHubAnnotation,
  ArtifactManager,
} from '@dotsetlabs/cli';

// Detect CI environment
const env = detectCIEnvironment();
console.log(`Running on: ${env.provider}`);

// Determine exit code based on findings
const exitCode = determineExitCode(scanResult, { failOnSeverity: 'high' });

// GitHub Actions: Write job summary
if (env.provider === 'github') {
  await writeGitHubSummary(scanResult);

  // Create annotations for findings
  for (const finding of scanResult.findings) {
    createGitHubAnnotation(finding);
  }
}

// Manage artifacts
const artifacts = new ArtifactManager({ basePath: './artifacts' });
await artifacts.write('sarif', sarifContent, { filename: 'mantle-results.sarif' });

process.exit(exitCode);
```

## Properties

### actor?

```ts
optional actor: string;
```

Defined in: src/mantle/ci/index.ts:157

### artifactsPath?

```ts
optional artifactsPath: string;
```

Defined in: src/mantle/ci/index.ts:162

### branch?

```ts
optional branch: string;
```

Defined in: src/mantle/ci/index.ts:142

### buildNumber?

```ts
optional buildNumber: string;
```

Defined in: src/mantle/ci/index.ts:152

### buildUrl?

```ts
optional buildUrl: string;
```

Defined in: src/mantle/ci/index.ts:153

### commit?

```ts
optional commit: string;
```

Defined in: src/mantle/ci/index.ts:143

### commitShort?

```ts
optional commitShort: string;
```

Defined in: src/mantle/ci/index.ts:144

### isCI

```ts
isCI: boolean;
```

Defined in: src/mantle/ci/index.ts:140

### isPR

```ts
isPR: boolean;
```

Defined in: src/mantle/ci/index.ts:141

### jobId?

```ts
optional jobId: string;
```

Defined in: src/mantle/ci/index.ts:155

### jobName?

```ts
optional jobName: string;
```

Defined in: src/mantle/ci/index.ts:156

### pipelineId?

```ts
optional pipelineId: string;
```

Defined in: src/mantle/ci/index.ts:154

### prNumber?

```ts
optional prNumber: string;
```

Defined in: src/mantle/ci/index.ts:148

### provider

```ts
provider: CIProvider;
```

Defined in: src/mantle/ci/index.ts:139

### prSourceBranch?

```ts
optional prSourceBranch: string;
```

Defined in: src/mantle/ci/index.ts:150

### prTargetBranch?

```ts
optional prTargetBranch: string;
```

Defined in: src/mantle/ci/index.ts:151

### prTitle?

```ts
optional prTitle: string;
```

Defined in: src/mantle/ci/index.ts:149

### repository?

```ts
optional repository: string;
```

Defined in: src/mantle/ci/index.ts:145

### repositoryName?

```ts
optional repositoryName: string;
```

Defined in: src/mantle/ci/index.ts:147

### repositoryOwner?

```ts
optional repositoryOwner: string;
```

Defined in: src/mantle/ci/index.ts:146

### runId?

```ts
optional runId: string;
```

Defined in: src/mantle/ci/index.ts:160

### runNumber?

```ts
optional runNumber: string;
```

Defined in: src/mantle/ci/index.ts:161

### summaryPath?

```ts
optional summaryPath: string;
```

Defined in: src/mantle/ci/index.ts:163

### triggeredBy?

```ts
optional triggeredBy: string;
```

Defined in: src/mantle/ci/index.ts:158

### workflowName?

```ts
optional workflowName: string;
```

Defined in: src/mantle/ci/index.ts:159
