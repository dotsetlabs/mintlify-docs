---
title: "Function: syncBaseline"
description: "```ts"
icon: "function"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / syncBaseline

# Function: syncBaseline()

```ts
function syncBaseline(
   baseline, 
   currentFindings, 
   options): {
  baseline: Baseline;
  diff: BaselineDiff;
};
```

Defined in: src/mantle/baseline.ts:496

Sync baseline with current findings
Updates lastSeen, reactivates resolved findings that reappear, etc.

## Parameters

### baseline

### currentFindings

[`SecretMatch`](../interfaces/SecretMatch.md) & \{
  `file?`: `string`;
  `line?`: `number`;
\}[]

### options

#### author?

`string`

## Returns

```ts
{
  baseline: Baseline;
  diff: BaselineDiff;
}
```

### baseline

```ts
baseline: Baseline;
```

### diff

```ts
diff: BaselineDiff;
```
