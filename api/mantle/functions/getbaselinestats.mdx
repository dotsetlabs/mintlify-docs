---
title: "Function: getBaselineStats"
description: "```ts"
icon: "function"
---

[Dotset CLI API Documentation](/api) / [index](/api/mantle) / getBaselineStats

# Function: getBaselineStats()

```ts
function getBaselineStats(baseline): {
  activeCount: number;
  averageOccurrences: number;
  byFile: Record<string, number>;
  byPattern: Record<string, number>;
  bySeverity: Record<FindingSeverity, number>;
  byStatus: Record<FindingStatus, number>;
  lastModified: string;
  newestFinding: string | null;
  oldestFinding: string | null;
  resolvedCount: number;
  totalFindings: number;
  versionCount: number;
};
```

Defined in: src/mantle/baseline.ts:759

Get comprehensive baseline statistics

## Parameters

### baseline

## Returns

```ts
{
  activeCount: number;
  averageOccurrences: number;
  byFile: Record<string, number>;
  byPattern: Record<string, number>;
  bySeverity: Record<FindingSeverity, number>;
  byStatus: Record<FindingStatus, number>;
  lastModified: string;
  newestFinding: string | null;
  oldestFinding: string | null;
  resolvedCount: number;
  totalFindings: number;
  versionCount: number;
}
```

### activeCount

```ts
activeCount: number;
```

### averageOccurrences

```ts
averageOccurrences: number;
```

### byFile

```ts
byFile: Record<string, number>;
```

### byPattern

```ts
byPattern: Record<string, number>;
```

### bySeverity

```ts
bySeverity: Record<FindingSeverity, number>;
```

### byStatus

```ts
byStatus: Record<FindingStatus, number>;
```

### lastModified

```ts
lastModified: string;
```

### newestFinding

```ts
newestFinding: string | null;
```

### oldestFinding

```ts
oldestFinding: string | null;
```

### resolvedCount

```ts
resolvedCount: number;
```

### totalFindings

```ts
totalFindings: number;
```

### versionCount

```ts
versionCount: number;
```
