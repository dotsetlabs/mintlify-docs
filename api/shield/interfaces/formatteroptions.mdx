---
title: "Interface: FormatterOptions"
description: "Defined in: src/shield/output/formatters.ts:88"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [index](/api/shield) / FormatterOptions

# Interface: FormatterOptions

Defined in: src/shield/output/formatters.ts:88

Output Formatting Module

Format scan results in multiple output formats for CI/CD integration.

## Example

```typescript
import { format, formatSARIF, formatMarkdown } from '@dotsetlabs/cli';

// Format as SARIF for GitHub Code Scanning
const sarif = format(scanResult, 'sarif');
fs.writeFileSync('results.sarif', sarif);

// Format as Markdown for PR comments
const markdown = formatMarkdown(scanResult, { includeContext: true });

// Format as JSON for API consumption
const json = format(scanResult, 'json', { prettyPrint: true });

// Get available formats
const formats = getSupportedFormats();
// ['text', 'json', 'sarif', 'csv', 'xml', 'junit', 'gitlab', 'github', 'markdown']
```

## Properties

### includeContext?

```ts
optional includeContext: boolean;
```

Defined in: src/shield/output/formatters.ts:89

### includeMetadata?

```ts
optional includeMetadata: boolean;
```

Defined in: src/shield/output/formatters.ts:90

### maxContextLength?

```ts
optional maxContextLength: number;
```

Defined in: src/shield/output/formatters.ts:92

### prettyPrint?

```ts
optional prettyPrint: boolean;
```

Defined in: src/shield/output/formatters.ts:91

### toolName?

```ts
optional toolName: string;
```

Defined in: src/shield/output/formatters.ts:93

### toolVersion?

```ts
optional toolVersion: string;
```

Defined in: src/shield/output/formatters.ts:94
