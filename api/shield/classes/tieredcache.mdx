---
title: "Class: TieredCache<K, V>"
description: "Defined in: src/shield/utils/cache.ts:568"
icon: "cube"
---

[Dotset CLI API Documentation](/api) / [index](/api/shield) / TieredCache

# Class: TieredCache\<K, V\>

Defined in: src/shield/utils/cache.ts:568

Multi-tier cache with L1 (fast/small) and L2 (slower/larger) layers

## Type Parameters

### K

`K`

### V

`V`

## Constructors

### Constructor

```ts
new TieredCache<K, V>(l1Options, l2Options): TieredCache<K, V>;
```

Defined in: src/shield/utils/cache.ts:572

#### Parameters

##### l1Options

[`LRUCacheOptions`](../interfaces/LRUCacheOptions.md) = `...`

##### l2Options

[`LRUCacheOptions`](../interfaces/LRUCacheOptions.md) = `...`

#### Returns

`TieredCache`\<`K`, `V`\>

## Methods

### clear()

```ts
clear(): void;
```

Defined in: src/shield/utils/cache.ts:614

#### Returns

`void`

### delete()

```ts
delete(key): boolean;
```

Defined in: src/shield/utils/cache.ts:608

#### Parameters

##### key

`K`

#### Returns

`boolean`

### get()

```ts
get(key): V | undefined;
```

Defined in: src/shield/utils/cache.ts:580

#### Parameters

##### key

`K`

#### Returns

`V` \| `undefined`

### getMetrics()

```ts
getMetrics(): {
  l1: CacheMetrics;
  l2: CacheMetrics;
};
```

Defined in: src/shield/utils/cache.ts:619

#### Returns

```ts
{
  l1: CacheMetrics;
  l2: CacheMetrics;
}
```

##### l1

```ts
l1: CacheMetrics;
```

##### l2

```ts
l2: CacheMetrics;
```

### has()

```ts
has(key): boolean;
```

Defined in: src/shield/utils/cache.ts:604

#### Parameters

##### key

`K`

#### Returns

`boolean`

### set()

```ts
set(
   key, 
   value, 
   ttl?): void;
```

Defined in: src/shield/utils/cache.ts:598

#### Parameters

##### key

`K`

##### value

`V`

##### ttl?

`number`

#### Returns

`void`
