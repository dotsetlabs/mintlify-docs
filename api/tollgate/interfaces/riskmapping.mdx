---
title: "Interface: RiskMapping"
description: "Defined in: src/tollgate/analyzers/types.ts:124"
icon: "brackets-curly"
---

[Dotset CLI API Documentation](/api) / [tollgate](/api/tollgate) / RiskMapping

# Interface: RiskMapping

Defined in: src/tollgate/analyzers/types.ts:124

Mapping from risk levels to policy actions.

Used in smart policies to define how each risk level should be handled.
Unspecified levels fall back to DEFAULT_RISK_MAPPING.

## Example

```typescript
const strictMapping: RiskMapping = {
  safe: 'allow',
  read: 'allow',
  write: 'prompt',
  destructive: 'deny',  // Stricter than default
  dangerous: 'deny'
};
```

## Properties

### dangerous?

```ts
optional dangerous: "allow" | "prompt" | "deny";
```

Defined in: src/tollgate/analyzers/types.ts:129

### destructive?

```ts
optional destructive: "allow" | "prompt" | "deny";
```

Defined in: src/tollgate/analyzers/types.ts:128

### read?

```ts
optional read: "allow" | "prompt" | "deny";
```

Defined in: src/tollgate/analyzers/types.ts:126

### safe?

```ts
optional safe: "allow" | "prompt" | "deny";
```

Defined in: src/tollgate/analyzers/types.ts:125

### write?

```ts
optional write: "allow" | "prompt" | "deny";
```

Defined in: src/tollgate/analyzers/types.ts:127
